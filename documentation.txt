1. install node

2. install web3: npm install web3
   if get error: 'node-gyp' then install node more than 11, and check chocolatey

3. Open PowerShell with admin and install two command

'npm install --global --production windows-build-tools --vs2015
 npm config set msvs_version 2015 �global'

4. 'npm install web3' =>done